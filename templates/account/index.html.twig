{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block content %}
    <h1>Mon compte</h1>
        {% if is_granted('ROLE_ADMIN') %}
            <small><a href="/admin">Acces gestion administrateur</a></small><br>
        {% endif %}
    Bienvenue {{ app.user.firstname }} dans votre compte. <br>
    C'est dans cet espace que vous allez pouvoir gérer toutes vos informations personnelles.
    <hr>
    <div class="row">
        <div class="col-md-4">
            <div class="account-item text-center">
                <a href="{{ path('account_password') }}"><i class="fas fa-lock"></i> Modifier mon mot de passe</a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <a href="{{ path('account_address') }}"><i class="fa-solid fa-address-book"></i> Gérer mes adresses</a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <a href="{{ path('account_order') }}"><i class="fa-solid fa-cart-flatbed"></i> Voir mes commandes</a>
            </div>
        </div>
    </div>
    <hr>

{% endblock %}
